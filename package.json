{
  "name": "toastique-inventory-tiny",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "vitest --run",
    "seed": "node scripts/seed.js",
    "dev:seed": "DB_PATH=db.sqlite npm run seed && nodemon server.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.6.0",
    "dotenv": "^17.2.1",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.0",
    "supertest": "^7.1.4",
    "vitest": "^3.2.4"
  }
}
